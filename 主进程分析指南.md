# 主进程 JS 深挖指南（补充篇）

> 针对"主进程是不是没什么值得分析的了"这个问题。结论：**恰恰相反**——之前只深挖了引擎 chunk（9HkvRynM），主进程还有三座没动的富矿，外加一处需要修正的结论。

---

## 1. eipc：982 个方法的命名空间 RPC 层（最大发现）

所有窗口 stub 里的 IPC channel 形如：

```
$eipc_message$_eaf88b14-ba52-4bb7-..._$_claude.web_$_WindowControl_$_resize
```

四段结构：`eipc_message` / 实例 UUID / **服务名** / `对象.方法`。全库去重后共 **982 个服务方法**。

主服务一览：
- `claude.web`（最大）：LocalSessions、WindowControl、WindowState、Resources、ManagedConfig、RemoteControlServing、PluginBridgeMcp、MenuEvents、SessionPopouts、QuickEntry、Toast、Navigation…
- `claude.settings`、`claude.internal.ui`、`claude.internal.findInPage`、`claude.hybrid`、`claude.buddy`

有意思的方法（抽样）：
- `LocalSessions.teleportToCloud` / `warmSession` / `submitPrReview` / `writeShellPty` / `toggleRemoteControl`
- `WindowControl.setIncognitoMode` / `captureScreenshot` / `reportModalScrim`
- `RemoteControlServing.requestEnable` / `requestServeSessionFolder`
- `ManagedConfig.managedRendererConfig_`（管控配置下发到渲染层的通道）

**安全封装**（`main-agent-sdk.pretty.js` L255628）：注册 handler 前统一校验 `senderFrame`，非法来源直接 `throw "Invalid IPC sender"`。这就是为什么直接 grep `ipcMain.handle(` 几乎搜不到业务 IPC——全走这层。

## 2. 121 个企业管控键，带官方文档字符串（相当于内部产品文档）

位置：`index.pre.pretty.js` 约 L21700–23600。分类分布：

| category | 数量 | 代表键 |
|---|---|---|
| connection | 52 | inference*（30+）、otlp*、bootstrap*、loginSsoOrgDomain |
| sandbox | 22 | requireCoworkFullVmSandbox、secureVmFeaturesEnabled、coworkEgressAllowedHosts、chatAdvancedFileAnalysisEnabled |
| telemetry | 16 | disableNonessentialTelemetry、disableEssentialTelemetry、otlpTracesEnabled |
| connectors | 11 | claudeInChromeEnabled、hardwareBuddyEnabled、isLocalDevMcpEnabled |
| source | 6 | claudeAiImport、endUserAttribution |
| appearance | 5 | banner、deploymentDisplayName |
| plugins | 3 | allowedPluginMarketplaces、orgPluginSettings |
| limits | 2 | inferenceMaxTokensPerWindow、mcpToolTimeoutSec |

重点键（引用原文文档）：
- `requireCoworkFullVmSandbox`："Runs tools inside an isolated VM instead of the host. Stronger isolation; slower file access and no host-process tools."
- 默认 host-native 模式的描述："Shell commands run inside the VM; file tools run on the host with path-based access control."
- `hardwareBuddyEnabled`："Allow pairing **Hardware Buddy Bluetooth devices** in Cowork."（buddy.js 窗口的用途实锤）
- `claudeInChromeEnabled`：扩展与 Desktop 双向"短时签名身份声明"配对；页面内容端到端直达，不经 Anthropic 服务器
- `inference*` 全家桶：Bedrock / Vertex / Foundry / Gateway 的 region、SSO、OIDC workforce、credential helper、token window、pricing multiplier

部署层级（文档明说）：macOS MDM / Windows `HKLM Policies\Claude` / Linux root `/etc`，用户级写入被忽略；服务端 control plane 还会强制下发某些键（如 Chrome 开关）。

## 3. index.pre.js = 应用"操作系统层"（33691 行 pretty）

- **Sentry 全家桶**（133 处引用）：L14816 起，双通道上报——eipc 风格 IPC（`start/scope/envelope/structured-log/metric`）+ 自定义协议，`index.js` 开头注入 SENTRY_RELEASE/DebugId
- **更新策略**：`disableAutoUpdates`、`autoUpdaterEnforcementHours`、`updateViaUpdatesHost`、"What's new"公告 UI 开关
- **CLAUDE_* 环境变量**（16 个）：`CLAUDE_AI_URL`、`CLAUDE_USER_DATA_DIR`、`CLAUDE_CODE_USE_BEDROCK|VERTEX|FOUNDRY|MANTLE`、`CLAUDE_CODE_ENHANCED_TELEMETRY_BETA`、`CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS`、`CLAUDE_CDP_AUTH`、`CLAUDE_GENERATE_COMPILE_CACHE`…
- **deep link**：`claude://` 注册（可用 `disableDeepLinkRegistration` 关闭）
- 权限/媒体/屏幕相关 35 处

## 4. 窗口 stub 群（都很小、职责单一，之前清单漏了一半）

| 文件 | 大小 | 职责 |
|---|---|---|
| buddy.js | 69KB | Hardware Buddy 蓝牙设备配对窗口（`claude.buddy/Buddy.{status,deviceStatus,setName…}`） |
| computerUseTeach.js / computerUseWatchRecord.js / watchRecordChooser.js | ~5KB×3 | CU 教学模式 / 操作录制回放选择 |
| localExecConsent.js | 1.4KB | 本地命令执行授权弹窗 |
| quickWindow.js | 152KB | QuickEntry 快速入口 |
| findInPage.js / aboutWindow.js | ~150KB | 查找 / 关于 |
| designWindow.js / claudePagePreview.js | ~1.4KB | 设计窗口 / 页面预览 |
| mainWindow.js / mainView.js | 180/251KB | 主窗口装配 |

## 5. session-media:// 自定义协议

`main-agent-sdk.pretty.js` L255537–255606：`protocol.handle` 注册，流式发送会话媒体文件；渲染侧对应 `readSessionMediaAsDataUrl` / `getSessionMediaStreamUrl`。

## 6. ⚠️ 修正之前的结论：Cowork 的沙箱模型

此前架构地图说"macOS 侧没发现 seatbelt 沙盒实质代码"。**修正**：Cowork 沙箱不走 seatbelt，而是 **VM 混合模型**——
- 默认 host-native 模式：shell 命令跑在 VM 里，文件工具跑在 host 上做**路径级 ACL**
- 可选 full VM 模式（`requireCoworkFullVmSandbox`）：整个 agent loop 进 VM
- 1p 版本另有 `secureVmFeaturesEnabled` VM 级隔离增强

这与 `claude-native-binding.node` 里的 VM 镜像增量缓存（`ApplyVmImageDeltaCacheOptions`）完全对上——那个 5.5MB Rust 模块就是为快速启动/增量更新这个 VM 服务的。

## 7. 下一步值得挖的 Top 5

1. **导出完整 eipc 清单**：`perl /tmp/eipc.pl <build>/*.js`（脚本已留在 /tmp，可另存）
2. **121 键 schema 提取成 JSON/Markdown 表**——这是 Anthropic 官方文档级材料，比逆向猜测强太多
3. **LocalSessions.\***：Desktop 如何管理本地/SSH/云端会话与 teleport
4. **ManagedConfig.managedRendererConfig_**：管控配置从 MDM→主进程→渲染层的完整链路
5. **RemoteControlServing**：远程控制的 HTTP 服务暴露面（安全审计价值高）
