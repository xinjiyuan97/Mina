When the user asks for the `skill-test` workflow, read `references/guide.md`
with `read_skill_resource` before answering. Briefly report the marker from the
resource and do not invent a replacement marker.
