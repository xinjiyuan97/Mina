# Claude Desktop / Claude Code 引擎架构地图

> 样本：Claude.dmg → Claude.app 1.37937.3，agent-sdk 0.3.246
> 方法：asar 解包 + strings 语言指纹 + pretty 反混淆 + 符号/字符串交叉引用
> 所有"功能推断"均有字符串证据支撑；纯推测已标注。

---

## 0. 总体分层

```
┌─────────────────────────────────────────────────────────────────┐
│ L0 原生壳层  Claude(120KB启动器) + Electron Framework + Squirrel   │
├─────────────────────────────────────────────────────────────────┤
│ L1 主进程 JS (app.asar 38.4MB, V8 bytecode 缓存加速)               │
│    窗口管理 / IPC 桥 / 会话编排 / 服务发现                          │
├─────────────────────────────────────────────────────────────────┤
│ L2 Agent 引擎层  @anthropic-ai/claude-agent-sdk (即 Claude Code)   │
│    agent loop / 工具注册表 / 权限 / memory / skills / MCP          │
├─────────────────────────────────────────────────────────────────┤
│ L3 常驻 Worker 层  mcp-runtime / pty-host / file-index / ...      │
├─────────────────────────────────────────────────────────────────┤
│ L4 原生能力层  8+ 个二进制: Rust×3, Swift×3, Go×1, C++×2          │
├─────────────────────────────────────────────────────────────────┤
│ L5 资源层  bundled-skills / ion-dist(渲染UI) / MCP servers        │
└─────────────────────────────────────────────────────────────────┘
```

**进程模型**：主进程(Node) + 渲染进程×N(Chromium) + MCP 子进程 + worker 进程 + 原生 helper。

---

## 1. L1 主进程模块（`.vite/build/`）

| 文件 | 大小 | 职责 | 证据 |
|---|---|---|---|
| `index.pre.js` | 818KB | **主入口**。package.json `main` 指向它 | cowork=28, memory=22 |
| `index.js` | 638B | pre → 真 index 的加载跳板 | 文件内容 |
| `mainWindow.js` | 180KB | 主窗口（加载 ion-dist 的 claude.ai SPA） | BrowserWindow 模块 |
| `mainView.js` | 251KB | 主视图 WebContentsView 管理 | 同上 |
| `buddy.js` | — | Buddy 窗口（悬浮助手） | 渲染层 buddy_window |
| `quickWindow.js` | — | Quick 全局快捷窗口 | 渲染层 quick_window |
| `designWindow.js` | — | 设计画布窗口 | — |
| `findInPage.js` | — | 页内查找 | 渲染层 find_in_page |
| `claudePagePreview.js` | — | claude.ai 页面预览 | — |
| `computerUseTeach.js` / `computerUseWatchRecord.js` / `watchRecordChooser.js` | — | **CU 教学模式**：演示→录制→回放 | 文件名 |
| `localExecConsent.js` | — | 本地执行授权弹窗 | 渲染层 local_exec_consent |
| `aboutWindow.js` | — | 关于页 | — |
| `coworkArtifact.js` | — | Cowork 工件 IPC 桥 | callMcpTool/askClaude/runScheduledTask |

**会话编排核心**（此前误标为"权限区"，已修正）：

| 文件 | 大小 | 真实职责 |
|---|---|---|
| `index2.chunk-Ct58ixob.js` | 674KB | **会话生命周期状态机**：事件序列 `entry→preflight→worktree(_hook/_checkout/_fetch/_ff/_add)→mcp→query→enqueue→init→first_...`（这是打点遥测的事件枚举，worktree=304 全是埋点名，不是逻辑复杂度） |
| `index2.chunk-DksJfvkE.js` | 406KB | Cowork 服务编排 + 定时任务(scheduledTask=98) + 权限转发(tool_permission=11) |
| `index2.chunk-CEDpjEqx.js` | 250KB | Cowork/Worktree 桥接 |
| `index.chunk-DidfJmAx.js` | 515KB | **claude-ssh 远程会话管理器**：管理 `userData/claude-ssh-remote/claude-ssh-<ver>` 二进制的下载/校验/启动（`binaryExists()`, `getVersionDir()`, 构建路径 `packages/desktop/claude-ssh/dist`）。内嵌 Sentry |

---

## 2. L2 Agent 引擎（Claude Code 本体）

> 核心事实：`loadAgentSdk()` 以 in-process 方式调 `query({prompt, options})`，`systemPrompt: {type:'preset', preset:'claude_code'}`——Desktop 直接复用 Claude Code 引擎，系统提示词由服务端下发（本地明文/字节码均未发现）。

| 文件 | 大小 | 职责 | 关键计数 |
|---|---|---|---|
| `index.chunk-9HkvRynM.js` | **5.6MB** | **引擎主包**：agent loop、工具注册表、权限协议、Cowork 编排 | cowork=382, mcp=104, scheduledTask=132, tool_permission=15, computer_use=29, skill=29 |
| `index.chunk-SxF_5_pd.js` | 1.4MB | SDK 运行时：LoopModel/SessionID/HookSchema/PermissionMode + subagent 调度 | subagent=23, memory=22 |
| `index.chunk-C4jFwWEz.js` | 1.35MB | SDK 辅助：memory(=23)+subagent(=11)（疑似 prompt/context 组装） | — |
| `compile-cache/*.jsc` | ~13MB | 上述 chunk 的 **V8 字节码缓存**（arm64+x64 双份），启动加速 | — |

**引擎内子系统定位**（基于 pretty 版 `main-agent-sdk.pretty.js` 8.2MB）：

| 子系统 | 位置 | 说明 |
|---|---|---|
| Agent 循环 | `query()` async generator | stop_reason: `tool_use`→执行工具→回填 `tool_result`→再请求；`end_turn` 退出。**无本地 while 主循环**（82742 行的 while(!0) 是打包进来的 fs 流库） |
| 权限协议 | pretty L216377-216809 | `canUseTool → {behavior:'allow'|'deny'}`；远程桥 `tool_permission_request/resolved`；会话拆除/重连时 **auto-deny** 待审请求 |
| Memory | SDK 内 `memoryDir` + `MEMORY` 引擎符号 | 本地 memory 文件支持（memoryDir=22 处） |
| Skills | `resources/bundled-skills/*.skill` + manifest.json | 6 个内置技能：docx/pptx/xlsx/pdf/pdf-reading/frontend-design |
| MCP | `mcp-runtime/` worker + `callMcpTool` | stdio+SSE 双传输（36 vs 6），MCP 工具按敏感度逐次授权 |
| Subagent | `subagent/AgentTool` 符号 | 子 agent 派生 |
| 沙箱(Cowork) | 引擎 cowork 符号 + `index2.chunk-DksJfvkE` | 沙箱工作区编排；Windows 侧有独立服务 `@ant/cowork-win32-service`；macOS 侧未见 seatbelt/sandbox-exec 实质代码（仅 1 处引用）——macOS 沙箱主要靠"授权制"而非沙盒机制（推断，已标注） |
| 定时任务 | scheduledTask=132 | runScheduledTask IPC + 引擎内调度 |
| Worktree | worktree_hook/checkout/fetch/ff/add | git worktree 自动管理 |
| 远程会话 | index.chunk-DidfJmAx + claude-ssh 二进制 | SSH 远程执行通道 |

---

## 3. L3 常驻 Worker（`.vite/build/` 子目录）

| Worker | 文件 | 职责 |
|---|---|---|
| `mcp-runtime/` | `directMcpHost.js`, `nodeHost.js` | MCP server 托管进程（stdio/SSE） |
| `pty-host/` | `ptyHostWorker.js` | 终端 PTY 宿主（node-pty） |
| `file-index-worker/` | `fileIndexWorker.js` | 文件索引（Glob/Grep 后端） |
| `heavy-work-worker/` | `heavyWorkWorker.js` + rolldown 打包 dist | 重计算任务（rolldown=Rust 打包器产物） |
| `shell-path-worker/` | `shellPathWorker.js` | 用户 shell PATH 探测 |
| `stall-sampler-worker/` | `stallSamplerWorker.js` | 事件循环卡顿采样（性能监控） |
| `transcript-search-worker/` | `transcriptSearchWorker.js` | 历史会话搜索 |

---

## 4. L4 原生二进制（语言已鉴定）

### `Contents/Helpers/`（独立可执行，Mach-O universal x86_64+arm64）

| 文件 | 大小 | 语言 | 证据 | 功能 |
|---|---|---|---|---|
| `app-cu-helper` | 1.9MB | **Rust** | 142×rust签名、cargo registry 路径 | **CU 输入注入守护进程**：JSON-RPC 服务端（`JsonRpcRequest`/`DispatchRawParams`）；动作集 `click/rclick/mclick/hover/key/scroll/type`；安全校验：AX 焦点窗口 containment 校验、`secure_input` 检测、`foreign_pid`/`permission_denied` 拒绝、`authEnvelope` 认证信封；**SkyLight 私有框架**操作窗口空间（`move_window_to_space`、`SLSGetFrontProcess`、`CGEventPost`） |
| `chrome-native-host` | 2.1MB | **Rust** (tokio 1.47.1 + anyhow 1.0.100, GitHub Actions CI 构建) | tokio/anyhow 源路径 | **Chrome Native Messaging 宿主**：标准 nm 协议（`Entering main message loop`/`Chrome disconnected (EOF received)`），桥接 Chrome 扩展 ↔ Desktop |
| `permission-fixer` | 204KB | **Swift** | `swift_FORCE_LOAD`×9、IOKit/XPC/Foundation | 权限修复小工具（名字+IOKit/XPC 推断为 TCC/权限辅助；具体逻辑待深入） |
| `disclaimer` | 136KB | **Swift** | Foundation、swiftCompatibility56 | **子进程包装器**：`Usage: disclaimer [--pgroup \| --ports-only] [--] <command>`——以进程组方式托管子进程并回收端口（受控 spawn） |
| `Claude iOS Sim.app/claude-ios-sim` | — | 待鉴定 | universal binary | iOS 模拟器辅助（开发/测试基建，随包泄漏） |

### `app.asar.unpacked/`

| 文件 | 大小 | 语言 | 证据 | 功能 |
|---|---|---|---|---|
| `@ant/claude-native/claude-native-binding.node` | **5.5MB** | **Rust (napi-rs)** | 177×rust签名、`napi_rs_async_work`、image-0.24.9、objc2-app-kit-0.2.2 | **屏幕采集/图像管线**：JPEG/PNG 编解码、NSBitmapImageRep 屏幕帧、`ApplyVmImageDeltaCacheOptions` **帧增量缓存**（只传变化区域）、safeStorage 加密存储 |
| `@ant/claude-swift/computer_use.node` | — | **Swift + NAPI** | swift_task_*、napi 符号 | CU 的 Node 侧 Swift 原生桥 |
| `@ant/claude-swift/swift_addon.node` | — | **Swift + NAPI** | 同上 | Swift 通用原生桥 |
| `node-pty/prebuilds/*/pty.node` + `spawn-helper` | — | **C++** | node-pty 官方预编译 | PTY 终端 |
| `office365-mcp/msal-node-runtime.node` + `libmsalruntime_arm64.dylib` | — | **C++/ObjC（微软官方 MSAL）** | MSAL 运行时 | Office365 OAuth 认证运行时 |
| `resources/github-mcp/github-mcp-server` | — | **Go 1.25.12** | `go1.25.12`、`runtime.Goexit` | GitHub 官方 MCP server（单文件静态二进制） |

### 语言统计
- **Rust ×3**：app-cu-helper、chrome-native-host、claude-native-binding（5.5MB）——Anthropic 明显在把性能/安全敏感层迁向 Rust（napi-rs 生态）
- **Swift ×3**：permission-fixer、disclaimer、claude-swift addons——macOS 系统集成层
- **Go ×1**：github-mcp-server——直接复用 GitHub 官方实现
- **C++ ×2**：node-pty（第三方）、MSAL（微软）
- **JS/TS**：全部业务逻辑（asar 内 ~30 个 chunk）

---

## 5. L5 资源层

| 路径 | 内容 |
|---|---|
| `resources/bundled-skills/` | manifest.json + 6 个 `.skill`（docx 162KB/pptx 166KB/xlsx 154KB/pdf/pdf-reading/frontend-design） |
| `resources/github-mcp/` | Go 版 GitHub MCP server |
| `resources/office365-mcp/` | office365-mcp.mjs（JS，Graph API：calendar=449/outlook=252/onedrive=17）+ pdf 提取 worker + MSAL |
| `ion-dist/` (70MB) | **渲染层 UI**：claude.ai SPA（React，`cds-root`=Claude Design System），主窗口直接加载 |
| `.vite/renderer/` | 6 个小窗口的独立渲染包：main_window/buddy_window/quick_window/about_window/find_in_page/local_exec_consent |

---

## 6. 关键数据流

```
用户输入(ion-dist SPA)
  → mainWindow IPC → index2.chunk-Ct58ixob 会话状态机(preflight→mcp→query)
  → loadAgentSdk() → 9HkvRynM 引擎 query() async generator
  → POST api.anthropic.com/v1/messages (SSE, systemPrompt=claude_code 预设由服务端下发)
  ← tool_use
  → canUseTool 权限检查(本地规则/弹窗/远程桥转发)
  → 工具执行:
      文件类 → file-index-worker / 直接 fs
      命令类 → pty-host(node-pty) 或受控 spawn(disclaimer --pgroup 包装)
      MCP 类 → mcp-runtime(directMcpHost) → github-mcp-server(Go) / office365-mcp(JS+MSAL)
      CU 类  → claude-native(Rust屏幕采集+帧增量) + app-cu-helper(Rust注入,AX校验)
               ↳ chrome-native-host(Rust) 支持 Chrome 扩展控制浏览器
  ← tool_result → 回填 → 下一轮循环 → … → end_turn
  → 会话持久化 + worktree 管理 + scheduledTask 定时调度
```

---

## 7. 模块 ↔ Claude Code CLI 对照

| Claude Code CLI (npm 包) | Desktop 内对应 |
|---|---|
| `cli.js` 单文件混淆包 | index.chunk-9HkvRynM.js + SxF_5_pd + C4jFwWEz（拆成 3 块） |
| --dangerously-skip-permissions | PermissionMode/bypassPermissions 符号 |
| Hooks | HookSchema/HookEvents/HookUrls |
| Skills (~/.claude/skills) | bundled-skills/*.skill + manifest |
| MCP (claude mcp add) | mcp-runtime worker + dxt-registry(@ant/dxt-registry) |
| TODO/Task 系统 | scheduledTask + TasksBridge |
| SSH 远程（CLI 无） | claude-ssh 二进制 + DidfJmAx 管理器（Desktop 增强） |
| 终端 UI (ink) | ion-dist React SPA（Desktop 替换为 Web UI） |

**结论**：Claude Code = 引擎循环(9HkvRynM) + 权限 + 工具注册表。Desktop 在其外包了：窗口系统、CU 原生层、远程执行、定时任务、Cowork 沙箱编排。想复刻 Claude Code，最小集是 9HkvRynM 的 query 循环 + canUseTool + 工具注册表。

---

## 附：未解之谜 / 后续可挖

1. `permission-fixer` 具体行为（需 Hopper/IDA 反汇编 Swift）
2. `claude-ios-sim` 的用途（测试基建泄漏进发布包）
3. `claude-native-binding` 5.5MB 中除图像外的功能面（可 napi 符号全量枚举）
4. 服务端下发系统提示词的完整抓包（mitmproxy + 自己账号，合法范围内）
5. `index2.chunk-DksJfvkE` 的 cowork 沙箱配置细节（Windows 侧有 win32-service，macOS 侧机制待确认）
